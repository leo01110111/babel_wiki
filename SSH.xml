<mediawiki xmlns="http://www.mediawiki.org/xml/export-0.11/" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.mediawiki.org/xml/export-0.11/ http://www.mediawiki.org/xml/export-0.11.xsd" version="0.11" xml:lang="en">
  <siteinfo>
    <sitename>CMU -- HPC Wiki</sitename>
    <dbname>mediawiki_2025</dbname>
    <base>https://wiki.babel.cs.cmu.edu/index.php/Main_Page</base>
    <generator>MediaWiki 1.44.0</generator>
    <case>first-letter</case>
    <namespaces>
      <namespace key="-2" case="first-letter">Media</namespace>
      <namespace key="-1" case="first-letter">Special</namespace>
      <namespace key="0" case="first-letter" />
      <namespace key="1" case="first-letter">Talk</namespace>
      <namespace key="2" case="first-letter">User</namespace>
      <namespace key="3" case="first-letter">User talk</namespace>
      <namespace key="4" case="first-letter">Project</namespace>
      <namespace key="5" case="first-letter">Project talk</namespace>
      <namespace key="6" case="first-letter">File</namespace>
      <namespace key="7" case="first-letter">File talk</namespace>
      <namespace key="8" case="first-letter">MediaWiki</namespace>
      <namespace key="9" case="first-letter">MediaWiki talk</namespace>
      <namespace key="10" case="first-letter">Template</namespace>
      <namespace key="11" case="first-letter">Template talk</namespace>
      <namespace key="12" case="first-letter">Help</namespace>
      <namespace key="13" case="first-letter">Help talk</namespace>
      <namespace key="14" case="first-letter">Category</namespace>
      <namespace key="15" case="first-letter">Category talk</namespace>
      <namespace key="250" case="first-letter">Page</namespace>
      <namespace key="251" case="first-letter">Page talk</namespace>
      <namespace key="252" case="first-letter">Index</namespace>
      <namespace key="253" case="first-letter">Index talk</namespace>
    </namespaces>
  </siteinfo>
  <page>
    <title>SSH</title>
    <ns>0</ns>
    <id>62</id>
    <revision>
      <id>466</id>
      <parentid>461</parentid>
      <timestamp>2025-10-22T18:19:46Z</timestamp>
      <contributor>
        <ip>172.18.0.1</ip>
      </contributor>
      <origin>466</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="5007" sha1="0a6ju7ppry0rndwprl14uupnqqkhyom" xml:space="preserve">= Request an Account =
* '''[https://docs.google.com/forms/d/e/1FAIpQLSccfWvXdBltL8oxPYEZPGD-IWTnXXPqQS2bwcHr72wpRi1l6A/viewform Account Requests]:''' Use this form to request an account or for requesting changes to an existing account.

= SSH Login =

To log in to a cluster, use the following command, replacing &lt;code&gt;&lt;username&gt;&lt;/code&gt; with your username and &lt;code&gt;&lt;mycluster&gt;&lt;/code&gt; with the fully qualified domain name (FQDN) of your cluster:
  ssh &lt;username&gt;@&lt;mycluster&gt;

&lt;span id="passwordless-login"&gt;&lt;/span&gt;

== Passwordless Login ==

If you want to bypass password authentication, you can use SSH key authentication. SSH keys allow you to connect to a server or multiple servers without entering your password each time.

To generate an SSH key pair, follow these steps:

&lt;ol&gt;
&lt;li&gt;On your local machine, generate an SSH key:&lt;/li&gt;
&lt;/ol&gt;

  ssh-keygen -t ed25519 -C "[$(whoami)@$(uname -n)-$(date -I)]"

&lt;ol start="2"&gt;
&lt;li&gt;Automatically add your public key to the server by running the following command (use your ANDREW password):&lt;/li&gt;
&lt;/ol&gt;

  ssh-copy-id -i ${HOME}/.ssh/id_ed25519.pub &lt;username&gt;@&lt;mycluster&gt;.

  # for windows, first navigate to your .ssh folder, then run:
  type .\id_ed25519.pub | ssh &lt;username&gt;@&lt;mycluster&gt; "cat &gt;&gt; .ssh/authorized_keys"

&lt;ol start="3"&gt;
&lt;li&gt;You can also configure your &lt;code&gt;~/.ssh/config&lt;/code&gt; file to avoid typing the full cluster address every time. Add the following lines to the file:&lt;/li&gt;
&lt;/ol&gt;

  Host &lt;mycluster&gt;
  HostName &lt;mycluster's hostname&gt;
  User &lt;username&gt;
  IdentityFile ${HOME}/.ssh/id_ed25519

&lt;ol start="4"&gt;
&lt;li&gt;Then, you can log in with the simple command &lt;code&gt;ssh ''mycluster''&lt;/code&gt;.&lt;/li&gt;
&lt;/ol&gt;

&lt;span id="shell-configuration"&gt;&lt;/span&gt;

== Connecting to a Compute Node ==

You can also ssh directly to a compute node where you have a job already running by doing:

 ssh -J babel babel-X-XX

More on [[Port Forwarding]]

== Copying Data to Compute Nodes ==

To copy data to compute nodes when the data directories are not directly accessible from the login nodes, you can utilize the login node as a JumpHost, allowing direct connections to the compute nodes. Follow these steps to configure SSH and establish the connection:

&lt;ol&gt;
&lt;li&gt;Launch an interactive job on a compute node.&lt;/li&gt;
&lt;li&gt;Determine the IP address of the compute node by executing the &lt;code&gt;ip a&lt;/code&gt; command on the compute node itself.&lt;/li&gt;
&lt;li&gt;Update your SSH configuration file located at &lt;code&gt;~/.ssh/config&lt;/code&gt; with the following entries:&lt;/li&gt;
&lt;/ol&gt;

  Host &lt;mycluster-short-name&gt;
    HostName &lt;mycluster-fqdn&gt;
    User &lt;username&gt;
    IdentityFile &lt;identityfile&gt;

  Host &lt;mycluster&gt;-compute-node
    HostName &lt;node ip addr&gt;
    User &lt;username&gt;
    ProxyJump &lt;mycluster&gt;

Replace the relevant items in &lt;code&gt; &lt; &gt;&lt;/code&gt; with your actual username, cluster name and the path to your SSH private key file.
For example, to connect to a node in the Babel cluster:

 Host babel
   HostName babel.lti.cs.cmu.edu
   User &lt;username&gt;
   IdentityFile /path/to/identityfile

 Host babel-compute-node
   HostName &lt;node ip addr&gt;
   User &lt;username&gt;
   ProxyJump babel

The updated configuration allows you to establish a connection to the compute node through the login node, acting as a JumpHost.

Refer to your cluster's dedicated page in the [[:Category:Clusters| Cluster's]] section for more detailed information.

&lt;span id="why-is-there-a-ssh-key-pair-in-home-dir-upon-first-login"&gt;&lt;/span&gt;

== Why is there an SSH key pair in the home directory upon first login? ==

When a user runs a job on two nodes, their script might need to SSH from node-1 to node-2. If the SSH connection fails or hangs, it is often because the fingerprint of the target node is not in the &lt;code&gt;~/.ssh/known_hosts&lt;/code&gt; file. To prevent this issue, we automatically create SSH keys and add the necessary fingerprints to &lt;code&gt;~/.ssh/authorized_keys&lt;/code&gt; when the user's home directory is created.

This process also eliminates password prompts when connecting to a node where you have a job running.

For more information on generating these keys, see: [[SSH#Passwordless_Login|Connecting to the Cluster - Passwordless Login]]

= Troubleshooting =

== Windows ==
=== SSH Connection Errors ===

When attempting to connect to the HPC cluster using Visual Studio Code and SSH, Windows users may encounter the following error:

 Corrupted MAC on input.
 ssh_dispatch_run_fatal: Connection to 128.2.205.73 port 22: message authentication code incorrect

==== Solution ====

To resolve this issue, add the following configuration to your local SSH config file (~/.ssh/config):

Host babel babel-login
    HostName babel.lti.cs.cmu.edu
    Ciphers aes128-ctr,aes192-ctr,aes256-ctr
    MACs hmac-sha2-256,hmac-sha2-512,hmac-sha1
    User &lt;USERNAME&gt;
    ForwardAgent yes

Replace &lt;USERNAME&gt; with your actual username.

If you are using an SSH key, add the following line, replacing the placeholder with your private SSH key's file path:

    IdentityFile C:/Users/&lt;USERNAME&gt;/.ssh/&lt;private_ssh_key_name&gt;</text>
      <sha1>0a6ju7ppry0rndwprl14uupnqqkhyom</sha1>
    </revision>
  </page>
</mediawiki>
