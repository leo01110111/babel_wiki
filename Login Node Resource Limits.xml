<mediawiki xmlns="http://www.mediawiki.org/xml/export-0.11/" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.mediawiki.org/xml/export-0.11/ http://www.mediawiki.org/xml/export-0.11.xsd" version="0.11" xml:lang="en">
  <siteinfo>
    <sitename>CMU -- HPC Wiki</sitename>
    <dbname>mediawiki_2025</dbname>
    <base>https://wiki.babel.cs.cmu.edu/index.php/Main_Page</base>
    <generator>MediaWiki 1.44.0</generator>
    <case>first-letter</case>
    <namespaces>
      <namespace key="-2" case="first-letter">Media</namespace>
      <namespace key="-1" case="first-letter">Special</namespace>
      <namespace key="0" case="first-letter" />
      <namespace key="1" case="first-letter">Talk</namespace>
      <namespace key="2" case="first-letter">User</namespace>
      <namespace key="3" case="first-letter">User talk</namespace>
      <namespace key="4" case="first-letter">Project</namespace>
      <namespace key="5" case="first-letter">Project talk</namespace>
      <namespace key="6" case="first-letter">File</namespace>
      <namespace key="7" case="first-letter">File talk</namespace>
      <namespace key="8" case="first-letter">MediaWiki</namespace>
      <namespace key="9" case="first-letter">MediaWiki talk</namespace>
      <namespace key="10" case="first-letter">Template</namespace>
      <namespace key="11" case="first-letter">Template talk</namespace>
      <namespace key="12" case="first-letter">Help</namespace>
      <namespace key="13" case="first-letter">Help talk</namespace>
      <namespace key="14" case="first-letter">Category</namespace>
      <namespace key="15" case="first-letter">Category talk</namespace>
      <namespace key="250" case="first-letter">Page</namespace>
      <namespace key="251" case="first-letter">Page talk</namespace>
      <namespace key="252" case="first-letter">Index</namespace>
      <namespace key="253" case="first-letter">Index talk</namespace>
    </namespaces>
  </siteinfo>
  <page>
    <title>Login Node Resource Limits</title>
    <ns>0</ns>
    <id>63</id>
    <revision>
      <id>431</id>
      <parentid>430</parentid>
      <timestamp>2025-02-28T17:18:30Z</timestamp>
      <contributor>
        <ip>74.98.250.13</ip>
      </contributor>
      <comment>/* Using VS Code on Compute Nodes */</comment>
      <origin>431</origin>
      <model>wikitext</model>
      <format>text/x-wiki</format>
      <text bytes="6387" sha1="914fr3dslo1ex47502dxcbp7je4yn0k" xml:space="preserve">__NOTOC__
Login nodes (e.g., babel-login-1 through babel-login-4) are built for job submission, editing scripts, and checking status; not heavy computing. To keep them fast for everyone, we set resource limits for the &lt;code&gt;hpcuser&lt;/code&gt; group. These limits aim to balance usability and performance. Intensive tasks can slow things down and impact other users if unchecked. For heavy work, use compute nodes, where limits are lifted.

__TOC__

== Current Limits ==
The resource limits below apply to &lt;code&gt;hpcuser&lt;/code&gt; group members on login all nodes. Each has a starting value (soft limit) and a maximum you can raise it to with &lt;code&gt;ulimit&lt;/code&gt; (hard limit):

{| class="wikitable"
|-
! Limit !! Soft Limit !! Hard Limit !! Description
|-
| Max open files || 4096 || 8192 || Covers files, sockets, and pipes per process
|-
| Max processes || 1000 || 2000 || Limits total processes and threads per user
|-
| Max locked memory || 8 MB || 16 MB || Amount of RAM a process can lock (rarely used)
|-
| Total memory || 4 GB || 8 GB || Virtual memory per process (code, data, stack)
|-
| Resident memory || 2 GB || 4 GB || Physical memory used per process
|-
| CPU time || 1 hour || 2 hours || Total CPU time per process
|}

These are sufficient to support tools like VS Code and Cursor in typical scenarios, such as editing code, managing small projects, or running lightweight extensions. However, they may not handle all use cases. Heavy workloads—like running VS Code with numerous resource-intensive extensions (e.g., TensorBoard, large language model plugins), opening massive workspaces with thousands of files, or processing big datasets—can push beyond these limits, especially for memory or file descriptors.

== Checking Your Usage ==
Use these commands to see how close you are to your limits. Each pair shows your current usage and the limit:

{| class="wikitable"
|-
! Resource !! Check Usage !! See Limit !! What It Means
|-
| Processes || ps -u $USER -o pid= &lt;nowiki&gt;|&lt;/nowiki&gt; wc -l || ulimit -u || Number of processes/threads (e.g., 500 of 1000)
|-
| Open Files || lsof -u $USER &lt;nowiki&gt;|&lt;/nowiki&gt; wc -l || ulimit -n || Open files or sockets (e.g., 1024 of 4096)
|-
| Locked Memory || cat /proc/self/status &lt;nowiki&gt;|&lt;/nowiki&gt; grep VmLck || ulimit -l || Locked RAM in KB (e.g., "VmLck: 2048 kB" = 2 MB of 8 MB)
|-
| Total Memory || pmap $$ &lt;nowiki&gt;|&lt;/nowiki&gt; tail -n 1 &lt;nowiki&gt;|&lt;/nowiki&gt; awk '{print $2}' || ulimit -v || Virtual memory in KB (e.g., 225628 = ~225 MB of 4 GB)
|-
| Resident Memory || pmap $$ &lt;nowiki&gt;|&lt;/nowiki&gt; tail -n 1 &lt;nowiki&gt;|&lt;/nowiki&gt; awk '{print $3}' || ulimit -m || Physical memory in KB (e.g., 6628 = ~6.6 MB of 2 GB)
|-
| CPU Time || ps -p $$ -o time &lt;nowiki&gt;|&lt;/nowiki&gt; tail -n 1 || ulimit -t || CPU time used vs. limit (e.g., "TIME 00:30:00" = 30 min of 1 hour)
|}

Run these in your shell. The "Check Usage" column shows what you’re using; "See Limit" shows your soft limit. Hard limits are in &lt;code&gt;cat /proc/self/limits&lt;/code&gt;.

== Adjusting Limits ==
You can raise soft limits up to the hard limit using &lt;code&gt;ulimit&lt;/code&gt; in your session:
* Increase open files: &lt;code&gt;ulimit -n 8192&lt;/code&gt;
* Increase processes: &lt;code&gt;ulimit -u 2000&lt;/code&gt;
* Increase locked memory: &lt;code&gt;ulimit -l 16384&lt;/code&gt; (in KB)
* Increase total memory: &lt;code&gt;ulimit -v 8388608&lt;/code&gt; (in KB)
* Increase resident memory: &lt;code&gt;ulimit -m 4194304&lt;/code&gt; (in KB)
* Increase CPU time: &lt;code&gt;ulimit -t 7200&lt;/code&gt; (in seconds)

These changes last only for your current session. You can’t exceed the hard limits without admin help—use compute nodes for bigger needs.

== Using VS Code on Compute Nodes ==
VS Code on login nodes can consume a lot of RAM at times (especially with extensions or TensorBoard). To avoid this, request a debug node and run VS Code there instead. For example, type &lt;code&gt;babel-9-11&lt;/code&gt; (or your babel-nodename) in the "Connect to Host" prompt in VS Code. This connects your VS Code to a compute node, where resource limits are much higher.

To set this up, add the following to your &lt;code&gt;~/.ssh/config&lt;/code&gt; file:

  Host babel
    HostName babel.lti.cs.cmu.edu
    User &lt;username&gt;
    IdentityFile ~/.ssh/&lt;ssh_key&gt;
    ForwardAgent yes
  Host babel-*
    HostName %h
    User &lt;username&gt;
    IdentityFile ~/.ssh/&lt;ssh_key&gt;
    ProxyJump babel

Replace &lt;code&gt;&lt;username&gt;&lt;/code&gt; with your username and &lt;code&gt;&lt;ssh_key&gt;&lt;/code&gt; with your SSH key file name (e.g., id_rsa). 

See [[SSH]] for more details on SSH configuration.

See [[VSCode]]

== Common Questions ==
Here are answers to common questions about these limits:

* '''Will my processes be killed?'''&lt;br&gt;Not immediately! If you hit a limit (e.g., open too many files or use too much CPU time), new actions will fail with an error (like "Too many open files" or "Resource temporarily unavailable"). Existing processes won’t be killed unless they try to exceed the limit after it’s reached. Move long-running or resource-heavy tasks to compute nodes to avoid this.

* '''Can I increase these limits myself?'''&lt;br&gt;You can bump soft limits (e.g., from 4096 to 8192 files, 8 MB to 16 MB locked memory, 4 GB to 8 GB total memory) with &lt;code&gt;ulimit&lt;/code&gt; in your session, but hard limits (e.g., 8192 files, 16 MB, 8 GB, 2 hours CPU) are fixed. For anything bigger, submit a job to a compute node.

* '''How do I check my usage?'''&lt;br&gt;Use &lt;code&gt;ulimit -a&lt;/code&gt; to see your limits. Check open files with &lt;code&gt;lsof -u $USER | wc -l&lt;/code&gt;. See memory limits with &lt;code&gt;cat /proc/self/limits&lt;/code&gt;.

* '''What if I need more resources on the login node?'''&lt;br&gt;Login nodes aren’t built for this. Submit a job to a compute node—they have higher limits.

* '''Will my tmux sessions be killed?'''&lt;br&gt;No. tmux itself uses very little CPU time; like just a few minutes over several hours and should typically be well under the 1-hour CPU limit. If you run lots of commands inside tmux (e.g., 50+ subprocesses or a script using &gt;4 GB), those might fail—or hit the CPU limit after 1 hour—affecting specific panes.

See the [[FAQ]] for more commonly asked questions. 

== Need More Resources? ==
Login nodes aren’t for heavy work. Submit jobs to compute nodes—they have higher limits (e.g., 131072 files, unlimited memory and CPU). Contact ans Admin if you’re stuck.

If these limits feel too tight, let us know your specific needs—we can tweak them to keep things smooth.</text>
      <sha1>914fr3dslo1ex47502dxcbp7je4yn0k</sha1>
    </revision>
  </page>
</mediawiki>
